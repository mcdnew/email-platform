services:
  proxy:
    image: caddy:2
    restart: always
    depends_on:
      - frontend
      - backend
    ports:
      - "80:80"
      - "443:443"
    environment:
      PUBLIC_HOST: ${PUBLIC_HOST:-localhost}
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER:     ${POSTGRES_USER:-email_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB:       ${POSTGRES_DB:-email_platform}
    volumes:
      - db_data:/var/lib/postgresql/data

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    depends_on:
      - db
    env_file:
      - .env
    environment:
      DATABASE_URL: "postgresql://${POSTGRES_USER:-email_user}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-email_platform}"
    volumes:
      - ./logs:/app/logs
      - card_images:/app/card_images
      - voice_notes:/app/voice_notes

  worker:
    build:
      context: ../outreach-bot
      dockerfile: Dockerfile
    restart: always
    depends_on:
      - backend
    env_file:
      - ./worker.env
    environment:
      CORE_PLATFORM_URL: "http://backend:8000"
      CORE_PLATFORM_PUBLIC_URL: ${CORE_PLATFORM_PUBLIC_URL:-}
      CORE_PLATFORM_API_KEY: ${API_KEY}
      UNSUBSCRIBE_SECRET: ${UNSUBSCRIBE_SECRET}
      OUTREACH_DB_PATH: /data/outreach.db
      CAMPAIGNS_DIR: /data/campaigns
    volumes:
      - ./worker-data:/data
      - worker_state:/root/.config/hailing

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: always
    depends_on:
      - backend
    environment:
      API_URL: "http://backend:8000"
      NODE_ENV: "production"

volumes:
  caddy_data:
  caddy_config:
  db_data:
  card_images:
  voice_notes:
  worker_state:


## usage:
# docker compose down
# docker compose build
# docker compose up -d
